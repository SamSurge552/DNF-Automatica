# 录制 → 训练对齐

组过图数据集时按本文做，不要在对话里「凭记忆续写」。

**磁盘代码才是当下实现。** 若 `export_dataset.py` / 写盘与本文打架：助手先向用户确认「改文档还是改代码」，**不要擅自改本文去迁就一次对话里的临时代码，也不要擅自改代码去迁就过时段落。**

---

## 用词（先消除歧义）

「导出」曾同时指两件事，这就是把清洗提前塞进写盘/组集的原因。以后只用下面三个词：

| 词 | 从哪到哪 | 不是 |
|----|----------|------|
| **写盘** | PNG → `images/<时间戳>/`；键+帧 jsonl（含 YOLO 框）→ `recordings/<地下城>/<时间戳>_<角色>/` | 不是组集 |
| **回放** | 读 jsonl，勾选试变换 | 不写盘、不组集 |
| **组集** | jsonl → `dataset_export/`（`export_dataset.py`） | 成品训练数据。脚本文件名仍叫 export，口头/文档说**组集** |

下文若出现「导出」，**只等于组集**，不等于写盘。

---

## 0. 三层数据（已定流程）

| 层 | 是什么 | 允许做什么 |
|----|--------|------------|
| 写盘 jsonl | 键 + 检测帧（`t_ns`/`png`/`player_xy`/`mon_xy`/计数/`infer_ms`） | 只写看见的。采集当场跑 YOLO。`player_xy` 可 `null`。不填、不相对、不筛门方向 |
| 回放 `fsm_replay.py` | 对照刷图、试清洗 | 勾选「沿用上次 player」「相对坐标」等。**试，不写盘** |
| 组集 `dataset_export/` | **成品训练数据** | 只应用**用户已确认**的清洗。未确认的变换禁止进组集 |

清洗逻辑（player 填充、相对坐标、原点左下/左上、填充上限…）必须先在回放里对着真实段看过，用户点头后再写进本节「已确认的组集步骤」，然后才改 `export_dataset.py`。  
回放是眼睛，组集是定稿；没有看过就组集等于把猜测冻进训练集。

`held_frac` / 去 auto-repeat / 相邻完全相同特征丢后一帧 / 丢掉无左端点第一帧：见第 4 节，**已确认**可进组集。

---

原始 jsonl **保持原样**（含 `player_xy: null`、含键盘自动重复 press、含特征完全相同的相邻帧）。

相关代码：`operation_analysis_module.py`、`yolo_engine.py`、`record_writer.py`、`screen_capture.py`、`export_dataset.py`（组集）、`shift_align_check.py`、`fsm_replay.py`、`relative_xy.py`（仅回放转换，组集不调用）。  
组集：`python export_dataset.py` → `dataset_export/samples.jsonl` + `manifest.json`（不改原始 jsonl）。  
对齐验证：`python shift_align_check.py` → val R² vs K（见第 7 节）。

---

## 1. 磁盘上有什么

```
images/<时间戳>/          PNG
recordings/<地下城>/<时间戳>[_角色]/
  meta.json
  keys.jsonl     {"t_ns","type":"press|release","key"}
  frames.jsonl   {"t_ns","png","player_xy","mon_xy","loot_xy","gate_xy","boss_xy","mon","loot","gate","boss","infer_ms"}
```

- 坐标（旧段 / 离线 YOLO 之后）是检测框中心，相对**截屏图**（与 YOLO 一致；图左上、y 向下）。player 只留置信度最高的一个；其它类是点列表。旧段可能没有 `*_xy`。
- **采集：PNG 进 `images/`，jsonl 进 `recordings/<地下城>/<时间戳>_<角色>/`。采集当场跑 YOLO，frames 与旧段同一套检测字段。** 组集仍只应用已确认清洗。
- 采集点「开始」即写盘，不经 OCR 进图。角色名来自 GUI 下拉（`character_names_custom.txt`），不是 OCR。
- 采集必须管理员启动；3 秒内无新按键则删段。

---

## 2. 时钟与打戳误差（实测，不要当 10ms）

键事件和视觉帧都用**同一进程**的 `time.time_ns()`，可以对齐。

| 流 | 何时打 `t_ns` |
|----|----------------|
| keys | pynput 回调里立刻打（误差可忽略） |
| frames | `capture()` **整段返回之后**才打：**不含** YOLO 推理 |

本机 1600×900，`capture(save=False)` 墙钟（warmup 后；脚本 `measure_capture_latency.py`）：

| 后端 | 中位 | 范围 | 备注 |
|------|------|------|------|
| ImageGrab + numpy→BGR（旧） | **41 ms** | 34–55 | 30 次；打戳明显偏晚 |
| **dxcam**（2026-08-31，n=40） | **8.35 ms** | 7.76–16.44 | p90≈8.7；当前默认 |

dxcam 墙钟已落在约 10ms 量级，**对齐仍用磁盘 `t_ns`，不做补偿**。  
`get_latest_frame` 可能返回已缓冲的上一帧：采集比游戏渲染快时，同一张画面会配两个时间戳。  
这在 **jsonl 里要留下来**（连续相同特征 = 画面没动 / 可能卡住，给 FSM 用）。  
**已确认的导出清洗**：丢掉相邻且特征完全一致的后一帧，计入 `dup_skipped`；按键用剩下帧的真实 dt 对齐。录制模块禁止按特征去重。独占全屏失败回退 Pillow 时墙钟又回到 ~40ms。

YOLO `infer_ms`（约 10–20ms，首帧可达数百 ms）**不进入** `t_ns`，不影响键-帧对齐。

若再压误差：在 grab **之前**打 `t_ns`，或打戳后减去本次 grab 耗时。未改之前不要在训练里自行减 40ms（那是旧 Grab 数）。

---

## 3. player 缺失（磁盘原样；填充尚未确认进导出）

当前写盘：

- 本帧没有 `player` 框 → `player_xy` 为 **`null`**，没有前帧缓存。
- 推理忙 → **整行 frames 不写**（键仍在）。

**导出当前（已确认）：坐标原样拷贝**，`player_xy` 保持 `null`，带上 `mon_xy` 等列表（没有则 `[]`）。

下面惯性方案曾写入本文，**尚未经回放确认，不要写进 `export_dataset.py`。** 先在 `fsm_replay.py` 勾选「player 缺失沿用上次」对着段看；用户点头后再把步骤搬进第 4.3 节并改导出。

训练时若把 `null` 当监督，模型可能学「人可以消失」；过图是否需要连续位置，用回放判断，不要先猜。

候选（未确认）伪代码：

```
last = None
for frame in frames:  # 已按 t_ns 排序
    if frame.player_xy is not None:
        last = frame.player_xy
        xy, hold = last, 0
    elif last is not None:
        xy, hold = last, 1
    else:
        skip this frame for player-conditioned training
```

`mon/loot/gate/boss` **不要**惯性套用：怪和门会真的没了。相对 player、原点改左下角：同样先回放，未确认不进导出（兼顾以后要往不同方向跑的图）。

---

## 4. 按键标签（写盘仍是事件；组集曾用区间 held_frac）

**用途已变（2026-09-02，见 DECISIONS §1.0）：** 不再当「每键动作编码 / 过图训练 y」。

- 技能键：held_frac **废弃**。快捷栏瞬发，只要按下沿所在帧的时间戳。
- 移动键：保留，但是**按住毫秒**（执行参数），不是 0–1 比例。开环复现轨迹用这个。
- 分类：快捷栏重排后按键位即可区分技能/移动；时长只短期当辅助。

下面 4.1–4.3 的**算法**仍对：区间是 `(t_{i-1}, t_i]`，分母是真实 `dt = t_i - t_{i-1}`（纳秒），**不是帧数**。组集脚本目前仍输出 `held_frac` + `dt_s`；还原毫秒：`held_ms = held_frac × dt_s × 1000`。jsonl 不用重录。把组集改成直接写 `held_ms`（且只保留移动键）属于改已确认步骤，等确认后再动 `export_dataset.py`。

**不采用** 每帧 `GetAsyncKeyState` 当标签。**不再**用 0/1 多热。**不再**导出 `count`。原始 jsonl 仍只存事件。

名义间隔以 GUI 为准（默认 **0.05s**）；**计算用该帧真实 `dt = t_i - t_{i-1}`**。忙帧跳过或导出时去重后 dt 会变大，不要写死 50ms。

### 4.1 从事件重建按住（去重 auto-repeat）

按 `t_ns` 扫 `keys.jsonl`：

- 某 key 第一次 `press` 且当前未按住 → 开始一段 hold（一次**真实按下沿**）。
- 已按住时再来的 `press` → **忽略**（Windows 方向键连发）。
- `release` → 结束 hold。
- 段末仍按住 → hold 延续到本段最后一帧 `t_ns`。
- 键名统一（`left` / `right` / `space` / `alt_l` …）。
- **开录时**用 `GetAsyncKeyState` **只轮询一次**，写入 `meta.keys_held_at_start` + `started_t_ns`，并往 keys.jsonl 塞一条 `seed` press。导出时这些键从开录起就算按住，`held_frac` 能到 1.0；随后 auto-repeat 不新开 hold。这不是每帧轮询。

旧段没有 `keys_held_at_start` 时，开录前已按住的键仍要等到下一次 press 才看得到。

### 4.2 每一帧一个标签（主监督）

帧 `i` 的区间 `I_i = (t_{i-1}, t_i]`，`dt = t_i - t_{i-1}`。  
第一帧没有 `t_{i-1}` → **不进训练集**（或用 meta 开录时间当左端，不要捏造 dt）。

| 字段 | 范围 | 定义 |
|------|------|------|
| `held_frac[k]` | 0–1 | `k` 在 `I_i` 内按下时长 / `dt`。全程按住跑 → `1.0`；dt=50ms 只擦了 10ms → `0.2`。上一区间就按住、本区间仍按着 → `1.0`。 |

实现：每个 hold 与 `I_i` 求交，交叠时长累加 / `dt`。

```
left:  held_frac=1.00   # 延续长按跑
space: held_frac=0.20   # 点一下
```

组集现状仍写每键 `held_frac`（缺键当 0）。用途上只对**移动键**有意义；还原执行时长用 `held_ms = held_frac × dt_s × 1000`。技能键不要把比例当标签。

### 4.3 对齐步骤（仅导出过图训练集；不改 jsonl）

**已确认：**

1. 两流按 `t_ns` 排序。
2. 相邻帧特征完全一致则丢掉后一帧（`dup_skipped`）。录制文件里这些帧仍在。特征含 `player_xy`、计数、以及若有则含 `*_xy`。
3. 键按 4.1 重建 hold（去 repeat；`keys_held_at_start` 初始化）。
4. 坐标字段从 jsonl **原样拷贝**（含 `player_xy: null`）。
5. 每帧按真实 dt 写 `held_frac`；无左端点的第一帧丢掉。
6. 帧戳：磁盘 `t_ns` 原样用。dxcam 打戳前墙钟中位 ~8ms；旧 Grab ~40ms。暂不补偿。
7. 忙帧 / 导出去重后：dt 用实际时间差。
8. 太短的段不要进训练集。

**未确认（先回放，再问用户是否写入本节并改导出）：** player forward-fill、相对坐标、坐标原点、填充上限。

---

## 5. 这张图上的键频次（行为分布，不是采样偏差）

过图模型按 **地下城 × 角色** 训。深渊这张图通关就是以向右为主，`right` held 均值约 0.24、其它键 <0.04，这是**正确的行为分布**，不是「right 录太多了」。

同样内容再录十段，比例不会变，也不该变。不要为了把左/上/下/技能「补到均衡」去重录，更不要给罕见键加 loss 权重——那等于告诉模型「你该多按左」，在这张图里是错的。

技能键大量为 0：如果通关时本来就很少按，模型就应该很少按。要学的是「这张图上人怎么打」，不是「每个键出现次数一样」。

只有这些情况才需要另录，而且目标仍是**自然打法**，不是改比例：

- 换图 / 换角色（另一套行为分布，另开模型或另开切分）
- 这张图上确实会用、但整段里一次都没出现的键（覆盖不足，不是加权）
- 过短误切不要进训练集（默认 <8s 或 <20 帧）

---

## 6. 不要再做错

- 不要在录制回调里改写 `player_xy` 把 null 盖掉。
- 不要把未在回放里确认的清洗写进导出。
- 代码与本文冲突时，问用户改文档还是改代码，不要默默改。
- 不要用训练 loss 是否下降来当「对齐对了」。用第 7 节的平移扫描。
- 不要把键盘 auto-repeat 当独立动作。
- 不要每帧轮询键盘来当唯一标签（会丢掉两帧之间的点按）。开录时 `GetAsyncKeyState` **一次**只用来初始化已按住集合。
- 不要用 0/1 多热当主标签。不要再导出 `count`。技能不要用 `held_frac`；移动侧目标是按住毫秒，不是比例。
- 区间长度用相邻帧真实 dt，不要写死 50ms。
- 不要给这张图的罕见键加 loss 权重，也不要把「各键次数均衡」当采集目标。键频次是该图的行为分布。
- OCR 裁剪区不影响 YOLO；YOLO 坐标相对**整窗**。

---

## 7. 正向验证：标签平移扫描（val vs K）

不要用「训练 loss 降了」当对齐对了：错位的标签也能让小模型过拟合下降。

便宜的替代（脚本 `shift_align_check.py`）：

1. 用已导出的 `dataset_export/samples.jsonl`。
2. **按段**把标签整体平移 K 帧：样本 i 用特征 `x[i]`、标签 `y[i+K]`（不跨段）。K = -10 … +10，共 21 个小模型。
3. 段级划分 train/val（不要随机抽帧，否则泄漏）。
4. 画 **val R² vs K**（越高越好）。

怎么读曲线：

| 形状 | 含义 |
|------|------|
| **单峰且峰在 K=0** | 画面与 `held_frac` 对齐正常，不必为时钟再补偿 |
| **单峰且峰在 K=+N（N>0）** | 键晚于画面。Δ ≈ N × 名义间隔（默认 0.05s）。这就是人的反应延迟，可以记下来，过图模型用 `x[i] → y[i+N]` 或把 Δ 当已知滞后 |
| **单峰且峰在 K=-N** | 键早于画面。更像帧戳偏晚，键被对到了后一帧特征 |
| **平坦、无峰、R² 接近 0** | 对齐坏了，或 YOLO 结构化特征对按键几乎没信息量（别急着训大模型） |

约定：K>0 表示标签取**更晚**的帧（看见再按）。  
21 个小 MLP 在现有卡上是分钟级；先 `python export_dataset.py` 再 `python shift_align_check.py`。重录 0.05s 数据后再跑一次才有意义（旧 0.3s 段的 K=1 已经是 300ms）。
