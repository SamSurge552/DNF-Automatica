# 项目归档笔记：技能组/分布已落地；txt 再改，下一对话对照实现

> 已被同晚 `ARCHIVE_NOTE_2026-09-07-1.md` 接续：走近 / gaps_ms / mix_a 已落地。本文件保留 21：40 交接当时的状态。

**日期：** 2026-09-07 21:40

**工程：** `d:\Desktop\T\test`

**配套：** `FSM_DESIGN.txt`（现行，本晚再次更新）· `CONTEXT_HANDOFF.md` · `DECISIONS.md` §1.0 · 此前 `ARCHIVE_NOTE_2026-09-06-1.md`

---

## 1. 本轮做了什么

对照 **上一版** txt 已改代码（本会话 20：26）：分布全表最相似 + `S_pos`/`S_size` + 小怪范围框；提取技能组 `a>b>c`、组结束时刻、效率 100% 用框、分布取 `range_max`；主面板/回放两个 S 旋钮。采集 OCR 建段、回放删段、`FSM_TEST/states.jsonl` 仍在。

**本晚 txt 又改了，本会话按用户要求只交接、不改代码。** 下一对话打开 `FSM_DESIGN.txt` 对照磁盘落地，不要沿用交接里的「范围异常仍然释放」。

---

## 2. 已验证 / 试验中 / 已撤销

**已验证（上一版 txt 的代码路径已写完；未当实机通关事实）：**  
分布最相似 + 框尺寸；技能组合并；`range_max`；OCR 进图建 `recordings/`、回城停段；回放删本段；`states.jsonl` 只写不读。

**试验中：**  
叠图是否像素级对齐；`th_ms` / GX/GY / 点按 80–120 手感；`press_lag=1`；技能组 CAST 只打组内第一技能（整组连放未接）。旧 `skill_features` 结构仍能读，新表要重提后才有。

**已撤销（txt 已改，代码还按旧条，下一对话必须改掉）：**

| 旧（磁盘 / 交接曾写） | 新（`FSM_DESIGN.txt` 本晚） |
|----|----|
| 范围异常仍然释放 | 提示后用捡物同款移动走近，直到本帧最远敌对在范围 box 内再放 |
| 效率不足 100% 仍用最远敌对记范围 | 效率不足 100% **暂时不计范围**；只对记了范围的释放取 max |
| CAST 等待只用该槽 `hold_ms` | 技能组等待 **组结束时刻**（各技能按下+hold_ms 的 max） |
| 组只 CAST 第一技能、无间隔字段 | 组记录 `gaps_ms`（相邻按下间隔，多次取中位数），FSM 按此复现 |

---

## 3. 下一对话先做

1. 读 `FSM_DESIGN.txt`，按第 2 表改 `fsm_core.py` / `skill_feature_extract.py` / 蓝字 / 回放帮助。  
2. 改完 **重新提取本图**。  
3. 不要用 DECISIONS 第 4 节覆盖 txt。默认过图 YOLO 是 `mix_a`。不要动组集 fill/相对。

---

## 4. 怎么接着干

新对话读 `CONTEXT_HANDOFF.md` 第 1 节。磁盘代码是上一版 txt；现行草案是本晚 txt。回复简体中文。
