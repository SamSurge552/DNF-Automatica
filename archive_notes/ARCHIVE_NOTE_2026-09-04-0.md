# 项目归档笔记：FSM_DESIGN 大改，代码尚未跟

**日期：** 2026-09-04

**工程：** `d:\Desktop\T\test`

**配套：** `FSM_DESIGN.txt`（现行草案，覆盖本笔记之前的开打/前进/提取）· `CONTEXT_HANDOFF.md` 第 1 节

---

## 1. 本轮文档覆盖了什么

用户重写 `FSM_DESIGN.txt`。下一对话按 txt 改 `fsm_core` / 提取 / GUI，不要用本笔记里的旧执行当现行。

---

## 2. 磁盘上仍是旧实现（对照用）

- 前进：每帧严格过门坐标；TAP→空1帧→HOLD；过坐标后计 A；Y 对齐且 X 未到则立刻停 Y。
- 开打：整数「房间号」= MON+BOSS 群中心（截屏绝对）+ 数量，S% 相似；序列按次数；距离=到群中心；不够就接近；无兜底普攻。
- 提取：黄字消失后一帧算杀 MON；范围=群中心 dist 中位；F 默认 50；序列一律次数序。
- CD：地图 `map_tags`【重置】+ BOSS1→0 后第一个相似房。

---

## 3. 怎么接着干

新对话读 `CONTEXT_HANDOFF.md` 第 1 节 + 整份 `FSM_DESIGN.txt`。范围不够时 txt 写「尝试方案待定」——不要自己发明接近怪群。
