# DECISIONS.md — 架构决策与理由

> 与 `blueprint.txt`（长期意图）、`CONTEXT_HANDOFF.md`（当天状态）、`TRAIN_ALIGN.md`（对齐规范）并列。
> 本文件记录**为什么这么定**，以及**什么时候该重新考虑**。规范细节以 `TRAIN_ALIGN.md` 为准。
> **实现与 TRAIN_ALIGN 打架时：先问用户改文档还是改代码。**

日期：2026-09-01
项目根目录：`d:\Desktop\T\test`

---

## 0. 写盘 / 回放 / 组集（已定）

「导出」不要再用：它既像写盘又像组集，上次把相对坐标塞进组集就是被这个词带着走的。

- **写盘**：采集路径唯一 = 截图 + 键盘 + YOLO。PNG 在 `images/<时间戳>/`；`keys.jsonl` / `frames.jsonl`（含 `player_xy`/`mon_xy`/…）/ `meta.json` 写到 `recordings/<地下城>/<时间戳>_<角色>/`。
- **回放**：读 jsonl 试清洗，不写文件。采集段与旧段一样带 `*_xy`，可直接提技能特征。
- **组集**：`python export_dataset.py` → `dataset_export/`，成品训练数据。只含 TRAIN_ALIGN 已确认步骤（当前：held_frac、去 dup、丢第一帧、坐标原样）。组集仍要带检测特征的 jsonl。

```
写盘（原样） → 回放试变换 → 用户确认清洗 → 写入 TRAIN_ALIGN「已确认」→ 才改组集脚本
```

- `relative_xy.py` 不进写盘、不进组集。
- FSM / 卡住统计：读 jsonl，不读 `dataset_export/`。

理由：清洗是先验，切错了加数据也救不回来；回放成本低，把未看过的 fill/相对坐标冻进训练集成本高。

---

## 1. 核心想法

### 1.0 当前阶段定性（2026-09-02）

本条是**当前对照**：覆盖第 2 节阶段一里用「MLP → held_frac」当通关策略的推进方式。第 1.1 分层图仍是长期结构；**当前不走行为克隆。** `blueprint.txt` 里的「过图小模型」仍是远期，不是现在在做的东西。

- **架构定性**：模块化 agent。YOLO+OCR 感知 → FSM 决策 → 快捷栏执行。不是从录像克隆按键时序。
- **当前路线**：开环回放优先跑通，再逐步闭环。
- **已确立原则**：
  1. **正向事件优于负向事件**——用出现 / 按下沿决策，不要靠松开、消失、长时间无事件来驱动。
  2. **能进数据表的别进代码**——键位、M/L/G、技能持续、冷却进 json；不要按角色写 `if`。持续是 `hold_ms`，冷却是 `cooldown_s`（秒），都写在键位表每个 skill 上（回放改毫秒、键位工具 OCR/手改，两边合并不丢）。旧字段 `hold_frames` 读入时 ×50。
  3. **按抽象层切分，不按角色切分**——感知 / 决策 / 执行共享；角色差异进键位表和动作词表。
  4. **新旧想法冲突 → 用新的，事后告知冲突。** 不要停下来问选哪个。组集脚本 vs TRAIN_ALIGN 已确认步骤仍先问。
- **已识别边界**：开环只适用于无战斗交互的速刷图。有战斗交互必须闭环（当前帧要进感知→决策→执行）。
- **待验证**（未测之前不要当事实）：
  - 按键分解后「未知操作」占比
  - 技能事件帧长分布
  - 状态 `frame_count` 分布
  - **旧批量从左扫到右 vs 逐步 `step` 是否等价**（见 §8；未测之前不要当事实）

**held_frac（2026-09-02，覆盖「训练 y = 每键 held_frac」）：**

- **技能键：废弃。** 快捷栏是瞬发点击，按住多久是手指噪声。技能事件只要「哪一帧按下」——一个时间戳，不是时长比例。
- **移动键：仍是核心，但是执行参数，不是动作编码。** 位移 ≈ 按住时长；开环复现轨迹靠这个。发给系统的是「按住 X 毫秒」。
- **存毫秒绝对值，不要存比例。** 比例还要再乘帧时长才能还原；帧率抖动会变成位置偏移。毫秒是按键的原生单位。
- **分类判定：短期可当点击 vs 长按的辅助；快捷栏重排后技能键和移动键基本不重叠，按键位分类即可，不必看时长。**
- **不用重录。** jsonl 已有 `t_ns`。现有组集若有 `held_frac` + `dt_s`，`held_ms = held_frac × dt_s × 1000`。组集脚本改成直接写 ms 之前先动文档、再改 `export_dataset.py`。

**核心层 / 双宿主（覆盖「单独三个 EXE」和把执行塞进回放）：**

- **核心（FSM + 决策）只有一份，必须是纯函数。** 文件：`fsm_core.py` 的 `step(snapshot, ctx, params)`。输入 = 检测快照 + context；输出 = `FsmDecision`（`state` / `flags` / `action` / `move_dir`，以及技能、CD、分布计数、流程步等展示字段）+ 新 context。卡住是状态 `卡住`（全局最高优先级，打断方法）；预热仍在 flags。不截屏、不发键、不读文件、不 `sleep`、不调墙钟。`t_ns` 必须单调不减（核心校验）；回放用 jsonl 原值，实机用截图完成时刻（推理前）。ctx 值语义，不就地改。
- **replay 宿主：** `fsm_replay.py` 读 jsonl → `run_track`（内部逐步 step）→ 可视化。
- **采集路径（唯一）**：截图 + 键盘 + YOLO。点开始即写盘；非管理员拒绝开始；3 秒无新按键删段。`frames.jsonl` 与旧段同一套检测字段。覆盖中间那版「采集不跑 YOLO」。采集 / YOLO测试 / FSM测试 / 自动化 **统一开 OCR 和进图判定**。
- **实机宿主：** `status_analysis_module` 截屏+YOLO → `step`（FSM测试）。FSM测试启动即发键（无勾选），由 `fsm_execute` 注入。核心仍不碰键盘。前进 / 开打 / 提取 / 怪物分布以 **`FSM_DESIGN.txt` 为准**。卡住：状态改为卡住，上下左右各 HOLD Y 帧（不是前进）。CAST 用键位表已勾选快捷栏的**单键** `command`（`bind_hotkey`；组合键不进序列）。全 CD 按住普攻 X（无 CD）。捡物 PICK = 左 Alt。过图文件与技能表在**进图前**加载；无技能表则中止 FSM测试/自动化。
- **防抖是因果的**（现行规则：只看当前帧和 ctx，不向后看）。「旧批量从左扫到右，与逐步 step 等价」是**断言，不是实测**——见 §8。开头 `max(M,L,G)` 帧打 `WARMUP`：初始 judged=False，这是预热不是两套算法不一致。已删除的 400 帧窗口才会在窗口左沿错误重启 debounce。

回放视觉草案仍以 `FSM_DESIGN.txt` 为准。不要用本文件第 4 节覆盖那份草案。

### 1.1 三层分层控制，但 FSM 是旁路不是串行层

```
检测器（眼睛）
    ↓
特征向量 ──────────────┐
    ↓                  ↓
    ↓                 FSM（慢脑，维护画面外的状态）
    ↓                  ↓ goal
    └──→ 网络（快脑：特征 + goal → 动作）
              ↓
         执行层（手：动作 → 带时序的按键序列）
```

**关键点：FSM 不在网络的上游，两者平级，都直接读检测器输出。**

- 错误结构：`if state == FIGHTING: keys = net(features)` —— FSM 当开关
- 正确结构：`goal = fsm.step(features); keys = net(concat(features, goal))` —— goal 当输入维度

理由：
- FSM 硬开关会把「边打边走」这类行为在数据里切碎，网络永远学不到
- 网络只看得见当前帧，看不见「已清 3 个房间 / 背包剩 2 格 / 打算走东路」这些**画面外的状态**
- goal 不是命令，是一条**压缩过的历史摘要**——是网络看不见的信息，不是剥夺它的决策权

**命名纠正**：网络就是脑子（运动皮层/小脑），不是「手」。分的是慢脑和快脑，不是脑和手。手 = 发按键的函数，里面没有任何决策。

### 1.2 分层是「用结构换数据」，不是零成本

分层的代价是真实的：**手工划分的层次边界本身是一个先验，切错了就是硬天花板，加多少数据都撞不破。**

特斯拉 FSD 的真实趋势是**从模块化转向端到端**（v12 用神经网络替换了大量手写规控代码）。拿它论证「应该分层」是举反了。

我们仍然分层，理由不是「分层更正确」，而是**数据效率**：特斯拉有几百万辆车采数据，我们有一个人和几天录制。在这个数据量级上，手工先验的价值远超它裁掉的策略空间。

**每一条硬编码规则都是一张欠条，数据多了可以逐条赎回。**

### 1.3 动作空间里放动作，不放键盘敲击

`双击跑`、`连招A` 是**动作类别**，由执行层展开成带时序的按键序列（时序参数手写死）。

理由：网络每帧独立输出各键概率，输出之间没有协调机制，**不可能稳定复现跨帧时序模式**（按下-松开-再按下，间隔要落在判定窗口内）。加多少数据都解决不了，问题在输出表示上。

副产物：一旦动作空间抽象对了，`count` 标签就没有存在必要。

### 1.4 输入可粗，输出必须保持控制频率

想让模型看到更长上下文 → **输入侧滑动窗口堆帧**（每帧都拼最近 K 帧特征，每帧都输出动作）。

不要用粗化输出来换上下文——那是拿反应速度换信息量，而这两样可以同时要。

### 1.5 感知统一，策略分图

- **一个 detector，所有图混合训练**。类别 = `player / mon / boss / loot / gate`
- **OCR 读图名 → 选择 FSM / 路线 / 战斗策略**，不选择 detector

理由：感知共性极高（同一引擎、同一渲染管线），策略共性极低（每张图路线机制不同）。

**类别扩展用数据解决，不用模型数量解决。** N 个模型 = 显存 ×N、延迟 ×N、每个模型见的数据更少、更新时要重训 N 次。

例外：某个 boss 特别难识别 → 加一条 map-specific 的**后处理规则**（几行代码），不是加一个模型。

### 1.6 能从 UI 拿的信息，永远不要从贴图学

血条、CD 图标、小地图、boss 房标记——都是引擎渲染的固定 UI，跨图完全一致，零训练成本，比视觉识别更准更早。

已知失效案例：boss 血条方案作废（不打不出现，且与小怪共用样式）。**替代方案：相位来自小地图房间位置（进图即可知），身份来自「boss 房 + 最大敌对框」。**

**远期：**「boss」是一个地图属性，不是一个视觉类别。  
**现行：** YOLO 仍有 `boss` 视觉类；FSM 用 `boss` 计数和坐标。小地图方案未落地。

---

## 2. 演进逻辑 / 技术路线

### 阶段一（当前）：一张图跑通

**通关标准（可测，不可模糊）：连续跑 10 次深渊，≥8 次完整通关回城，全程无人干预。**

标准是「能跑完」，不是「打得好」。慢 50% 无所谓，卡死一次归零。

**最小切片 = 现在就砍掉的东西**（每一样等出现症状再加）：

| 砍掉 | 何时加回 |
|---|---|
| goal / FSM 输入 | 看到门口来回抖 |
| 双击跑 | 基础闭环稳定后 |
| 滑动窗口堆帧 | 看到慢半拍或动作抖动 |
| `count` 标签 | 实测分布后决定（大概率删） |
| Poisson loss | 若 count 删了则不需要 |
| `right` 不均衡加权 | **第二阶段**（见下） |

**保留：** `solarwarden_b` 检测器（角色 SolarWarden 过图权重；旧名 v7）→ 单帧特征。后半段「MLP → held_frac」已被 §1.0 覆盖，当前不走行为克隆。

**两件不能砍：**
1. **兜底恢复**——训练数据里永远不会有（录制时不会卡住），必须硬编码
2. **成功率统计脚本**——单次运行方差大到能骗过直觉；没有它，「这个改动有用吗」无法回答

### 阶段二：跨图

- 处理 `right` 不均衡（**现在处理是错的**：深渊图本来就往右推进，24% 是正确的行为分布，不是采样偏差；现在加权等于教模型多按左键）
- copy-paste 合成数据（用已标注 bbox 裁出**真实渲染态** sprite 贴到其他图背景，打断 boss/怪与背景的相关性）
- boss 类别不均衡处理（深渊流程短，boss 帧可能只占 3~5%）

### 阶段三：跨角色 / 跨职业

- 玩家检测：**混合多角色数据训一个类**，不是每角色一个模型
- 零标注采集：空房间/训练场里唯一会动的是玩家 → 帧差分自动生成 bbox
- 跨职业迁移的关键分界：**共享部分（走位、威胁判断、状态抽象）vs 不共享部分（技能序列）**必须分开。分开了换职业就是换动作词表；没分开换图都不行。

> 人换职业慢主要是重学肌肉记忆——而那恰恰是模型最便宜的部分。所以「人学得慢」不构成「模型迁移难」的证据。

### 阶段四：VLM 慢回路

**VLM 不做主感知**（3B~8B 单次推理百毫秒到秒级，进不了战斗回路）。

VLM 唯一的活：**异常恢复层**——回答「我现在到底在哪个界面、有什么可点的按钮」。窄模型遇到没见过的弹窗必然沉默，VLM 至少能说「这有个关闭按钮，坐标在这」。

调用频率 ~1Hz，4-bit 量化 3B 约 3GB 显存，可与游戏共存。

---

## 3. 症状 → 该加什么

**跑起来之后按实际死因决策，不要提前猜。**

| 症状 | 原因 | 加什么 |
|---|---|---|
| 门口来回抖、走两步回头 | 同观测不同动作（标签矛盾） | goal 输入 |
| 总是慢半拍、抬手躲不掉 | 人类反应延迟未补偿 | 标签前移 K 帧（K≈9~15） |
| 动作高频抖动 | 单帧无上下文 | 滑动窗口堆帧 |
| 卡在某个动作不放 | copycat（动作自相关） | 减少动作类历史特征 / 对这些维度加 dropout |
| 清完怪不走 / 提前走挨打 | mon 漏检 | 补 mon 数据 + 触发条件时间平滑 |
| 通关太慢 | 只会走不会跑 | 双击跑作为动作类别 |
| 卡地形 | 恢复动作不够 | 扩充恢复序列 |
| 不会原地放技能 | 静止帧被去重丢掉 | 去重规则对技能帧放宽 |
| 换图后崩盘 | 背景泄漏 | copy-paste 合成打断背景相关性 |

---

## 4. FSM 设计（已定）

```
并行监视器（每帧，最高优先级，可打断任何状态）:
    player_xy 连续 N 帧几乎不变  → RECOVERY
    当前状态超时                  → RECOVERY

主流程（带迟滞）:
    1. 进图?              → 否: 等待
    2. MON / BOSS?        → 有: goal = FIGHT
    3. LOOT?              → 有: goal = LOOT（同一目标失败 3 次后拉黑）
    4. GATE (x > player)? → 有: goal = ADVANCE
    5. 都没有?             → 等待 / 探索，超时进 RECOVERY

RECOVERY（硬编码，不经过网络）:
    ESC 关窗口 → 换方向移动 → 跳跃 → 仍无效则返回城镇重开
    每次尝试必须与上一次不同（否则墙角死循环）
```

**三条相对初版的修改及理由：**

1. **卡住检测提到并行**——原设计放在第 5 步永远走不到：卡在墙角时 gate 通常恰恰可见（停在第 4 步），卡在怪堆时 mon 有（停在第 2 步）。**卡住可发生在任何状态下。**
2. **每个状态加超时**——没有超时的 FSM 遇到意外就是死循环。建议 FIGHT 60s / LOOT 10s / ADVANCE 20s。
3. **加迟滞**——`mon` 是 mAP 最差的类，清怪瞬间检测结果时有时无 → FSM 每帧横跳。
   - **进入条件用单帧，退出条件用连续 N 帧**（看到 1 帧 mon 就开打，连续 5 帧无 mon 才算清空）
   - 状态最小持续时间 0.5s
   - 不对称是故意的：误判有怪代价小（多打两下），误判没怪代价大（转身挨打）

**GATE 方向偏好**：深渊图向右推进，只选 `gate.x > player.x` 的门。否则从左门进来、左门仍可见 → 原路返回 → 无限往返。无状态 FSM 解决不了「我从哪来」，先试方向偏好，不够再加「已通过的门」列表（按方向记，不按坐标）。

**分层边界（写死，防止 Cursor 下次接错）：**
- `FIGHT / LOOT / ADVANCE` → goal，交给网络
- `返回城镇` → 纯符号操作，硬编码，**不经过网络**
- `RECOVERY` → 硬编码动作序列，**不经过网络**

---

## 5. 数据与标签决策

### 5.1 按键标签：记状态，不记事件

- 按 `t_ns` 扫 `keys.jsonl`，逐键状态机重建按住区间
- 已 down 时再来的 press = auto-repeat，**丢弃**
- **自检：重建后的区间数应等于 release 数**
- 未配对处理：尾部有 press 无 release → 用录制结束时间闭合；头部有 release 无 press → **丢弃该段**（不要反向补）
- **开录时已按住的键**：段开始时用 `GetAsyncKeyState` **轮询一次**初始化 held 状态表（只一次，不是每帧），否则第一个 auto-repeat press 会被当成真实按下沿，产生幽灵 count

### 5.2 损失函数：不用 MSE

| 标签 | 损失 | 理由 |
|---|---|---|
| `held_frac` | **BCE** | 天然是 0~1 概率量；两端梯度比 MSE 大，正是最需要分清的区域 |
| `count`（若保留） | **Poisson NLL** | 非负整数计数的标准做法；MSE 假设残差高斯对称等方差，三条全不满足 |

**MSE 在 count 上的具体失败**：95% 的窗口是 0，均值 0.06 → 模型最优策略是永远输出 0.06，loss 很低但什么都没学会。且连续输出（如 0.4）需要额外阈值，不如一开始就分类。

**离散、取值范围小的东西 → 分类；不要回归。**

### 5.3 `count` 的存废：待实测

`count` 存在的**全部理由**是「视觉 200ms 一帧，0/1 会把连点两下、擦边一下、死死按住糊成同一个 1」。

**这个前提已被推翻**：dxcam 实测打戳前墙钟中位 8.35ms，YOLO 推理 14.7ms，实跑达到 18.5Hz（帧间隔 54ms，p90 55ms）。

54ms 帧间隔下，双击的两次按下间隔（80~150ms）**大概率跨帧**，count 退化成 0/1，与 held_frac 上升沿高度重合。

**待办：录到键盘数据后第一件事——统计 count 分布。若 99% 落在 0/1，删掉 count，只留 held_frac + BCE，标签方案砍一半。**

### 5.4 标签粒度不可逆

录制和标签一律用最高可用粒度。**推理频率是运行时旋钮，随时能调；标签粒度是不可逆的。**

当前 0.05s 采集实测稳定（18.5Hz），不必再往 30ms 压。

### 5.5 player 填充 / 相对坐标（未确认进导出）

漏检填上一帧、相对 player、原点左下角：先当回放选项。深渊主要向右，相对坐标以后可能对「要往不同方向跑的图」有用，**现在不冻结进成品**。

若以后确认填充：不要无限 fill。实测漏检曾到连续约 20 帧（~1s），把 1 秒前的点当现在是错数据。候选：`player_hold` 超过 5 帧就丢该帧。须回放看过再写入 TRAIN_ALIGN 已确认步骤。

### 5.6 其他数据规则

- **dxcam 重复帧**：`get_latest_frame` 可能返回缓冲的上一帧 → 同一画面配两个时间戳、两组按键标签 = 同输入不同标签。导出时按特征向量完全一致判定并丢弃，manifest 记 `dup_skipped`。
- **`mon/loot/gate/boss` 不做惯性填充**（怪和门会真的消失），只有 player 适合 hold。
- **静止帧去重的副作用**：站着不动时的所有操作（原地放技能）会被一起丢掉。深渊图影响小，但若出现「不会原地放技能」，原因在这。
- **标注规则文档**：跨时间合并数据集最常见的翻车不是灾难性遗忘，是**标注漂移**（框松紧、边界规则不一致）。表现是精度莫名卡住。写一份几行的规则文档，比多标 500 张有用。

### 5.7 增量训练 vs 重训

三个选项，中间那个是要的：

- ❌ 只拿新数据接着训 → 灾难性遗忘
- ✅ **合并新旧数据，从 `solarwarden_b` 权重热启动**
- ✅ 合并新旧数据，从 COCO 预训练权重重训（更干净可复现）

在当前量级（几千张图、5070 Ti、几十分钟一轮）**默认合并全量重训**。等训练要跑一整天了再考虑增量。

### 5.8 换新图先测再训

新图先直接跑 `solarwarden_b` 测每类精度。大概率结果：boss 完全失败，其他掉一点但可用。**只有测完才知道该补什么**（可能只需补 boss，也可能新图地面纹理让 loot 误检暴增）。这个信息现在免费，训完再发现就贵了。

### 5.9 预标注（模型在环）

- 工具：X-AnyLabeling 支持加载自定义 ONNX（yaml 配 model_path / conf / iou / classes）；导出 ONNX **不要加 `--dynamic`**
- 更推荐脱耦：`model.predict(save_txt=True, save_conf=True, conf=0.15)` 输出 YOLO 格式，再用工具改
- **阈值要低（0.15~0.25），不要用默认 0.45**——删一个多余框比画一个漏框快得多，成本不对称
- **真正省时间的是选帧不是预标注**：先 phash/帧差分去重，再按不确定性排序（conf 落在 0.3~0.6、相邻帧检测数突变的优先标）。标 200 张高信息量帧 > 标 2000 张随机帧
- **自我强化陷阱**：修改比从零画更容易锚定已有框，系统性偏差会逐代放大。防护：回归集永远纯人工；每隔几版随机抽 30~50 张从零手标对比

---

## 6. 评估基础设施（优先级最高，先于任何模型改进）

**单人的真正瓶颈不是算力、不是模型，是评估循环的墙钟时间。**

1. **回归测试集**——每张图、每个角色留一小批**永不参与训练**的数据，每版跑一遍全部。没有它，合并数据集时永远在赌（精度涨了还是某个旧场景悄悄崩了，无从知道）。
2. **成功率统计脚本**——每次跑完自动记录：是否通关、耗时、触发几次恢复、死在哪个阶段。
3. **版本号绑数据集，不只绑模型**——每版记清单：哪些图、哪些角色、哪些外观、标注规则版本。三个月后这个信息决定能否诊断回归。
4. **抽样调试图**——新采集已存全部 PNG。旧「不存 PNG」段仍无法事后审计检测质量。`mon` 仍是 mAP 最差的类且是 FSM 触发条件的核心输入。

### 对齐验证方法

「不要用训练 loss 是否下降当对齐对了」——替代方案：

**把标签整体平移 K 帧（K = -10…+10），各训一个小模型，画 val 指标 vs K 的曲线。**

- 单峰在 K=0 → 对齐正确
- 单峰在 K≈+9~15 → 捕捉到人类反应延迟 Δ（顺手估出来了）
- 曲线平坦无峰 → 对齐错了，或特征没有信息量

一个实验同时验证对齐、估出 Δ、检查数据有效性。21 个小模型在 5070 Ti 上是分钟级。

---

## 7. 工程与硬件

- **GPU 完全够**：5070 Ti 16GB。YOLO26n 训练分钟到小时级；推理个位数毫秒；4-bit 3B VLM 约 3GB
- **瓶颈在内存和数据管线**，不是显卡：预缩放存盘、memmap/LMDB、worker 数调到 4。**软件侧没榨干之前不要买内存**
- **训练与推理不同时发生**——峰值内存需求是分离场景，不叠加
- B850M 只有 2 槽已插满，升级必须整套换；先跑起来看实测峰值再决定
- **admin 硬拦截（采集路径已做）**：采集开始前检查进程已是管理员；开录 3 秒内 `keys.jsonl` 无新按键事件则弹窗中止并删除该段目录，不给「继续采集」选项。按钮提醒挡不住手滑，所以挂在唯一采集路径上。
- **`meta.json` 的 `completed`：暂缓。** 作用未清，稍后再研究（§8 仍挂着）。

---

## 8. 待解决的问题

### 阻塞项
- [ ] **键盘数据未录到**（2026-08-31 180010 段忘开管理员）→ 该段 1849 帧训练价值为零，仅验证视觉管线
- [x] admin 硬拦截（采集路径：非管理员拒绝开始；3 秒无新按键删段）
- [ ] 重录一段完整通关（带键盘）

### 待验证
- [ ] **`count` 分布**——18.5Hz 下是否 99% 落在 0/1？决定 count 存废
- [ ] **对齐是否正确**——平移实验未做
- [ ] **人类反应延迟 Δ**——未估（平移实验顺带产出）
- [ ] **`mon` 真实召回率**——无 PNG 无法审计；20% 帧有 mon，是本来就少还是漏了一半？
- [ ] 小地图 boss 房标记方案是否可行（当前仍用 boss 视觉类，需确认 UI 可读性）
- [ ] **按键分解的未知操作占比**——快捷栏 / 跑 / alt 捡物之外还剩多少对不上
- [ ] **技能事件帧长分布**——按下后持续几帧；是否该按技能分，还是先共用
- [ ] **状态 `frame_count` 分布**——开打 / 捡物 / 前进 / 空闲各待多久；超时和 M/L/G 不能拍脑袋
- [ ] **旧批量 debounce 与逐步 `step` 是否等价**——§1.0 那句是断言不是实测。必须对照已删除的从左扫到右批量实现：若原实现有任何向后查看（用未来帧才改 `judged`），所有「回放对比基准」失效，不能拿旧批量结果当逐步 `step` 的对照。

### 待实现
- [ ] forward-fill / 相对坐标：回放确认后再进导出（填充若做，上限 5 帧仍是候选）
- [ ] dxcam 重复帧去除 + `dup_skipped` 统计
- [ ] `meta.json` 的 `completed` 字段（**暂缓**，作用未清）
- [ ] 抽样调试 PNG
- [ ] 损失函数换 BCE（+ Poisson，若保留 count）
- [ ] 回归测试集
- [ ] 成功率统计脚本
- [ ] 标注规则文档
- [ ] FSM：状态超时 + 迟滞 + gate 方向偏好（卡住已按 `FSM_DESIGN.txt`：全局最高优先级、状态=卡住）
- [ ] 清洗：回放确认后再写入导出（fill / 相对坐标 / 原点）
- [ ] RECOVERY 硬编码序列

### 已知但推迟（第二阶段）
- [ ] `right` 不均衡（**现在处理是错的**）
- [ ] 技能键样本几乎为 0
- [ ] copy-paste 合成数据管线
- [ ] boss 类别不均衡

---

## 9. 方法论备忘

几条在这次讨论里反复用到、值得带走的判据：

1. **凡是把一个工程选择说成零代价的论述，都在隐藏交换条件。** 真实结论几乎总是「用 A 换 B，在当前约束下这笔交换划算」。缺的那句永远是「什么条件下不划算」。
2. **把捆绑的提案拆开单独评估。** 一个方案里的两个改动，常常一个该收一个该扔（例：软标签该收，200ms 粗化该扔）。
3. **判断「做得够不够好」的标准：这一步做得更好，会改变我下一步的决策吗？** 会 → 值得做深；不会 → 停手。
4. **不要按器官顺序逐个做完整。** 眼睛的规格由大脑的需求决定，而你现在还不知道大脑需要什么。薄的垂直切片先跑通，再看它死在哪。
5. **门槛过滤器筛掉的是耐心，不是能力或意图。** 对冲动型问题有效，对预谋型问题无效。
6. **先测再改。** 换新图先跑旧模型测精度；改帧率先实测分布；买硬件先看实测峰值。
7. **新旧想法冲突：用新的，事后告知。** 先按新想法改代码/文档，回复里写明撞了哪条旧约定。不要为了「选哪个」卡住。例外：组集脚本与 TRAIN_ALIGN **已确认**步骤打架，仍先问改文档还是改代码。

---

## 10. 交接

新开对话时：给出本文件 + `CONTEXT_HANDOFF.md` + `TRAIN_ALIGN.md` 路径。导出步骤以 TRAIN_ALIGN 第 0 节、第 4.3 节「已确认」为准。

- `blueprint.txt` = 长期意图
- `CONTEXT_HANDOFF.md` = 当天状态
- `TRAIN_ALIGN.md` = 对齐规范；与代码冲突则问用户
- `FSM_DESIGN.txt` = 回放/测试视觉草案（本文件第 4 节尚未落地，不准覆盖 txt）
- `DECISIONS.md`（本文件）= 决策与理由；**当前阶段定性见 §1.0**

**磁盘代码才是真相。**
